import { Dialog, withStyles } from '@material-ui/core';

export default withStyles({
  paper: {
    //backgroundImage: `linear-gradient(to right, #2ec1e4 0%, #e7ebc6 100%)`,
    background: 'white'
  },
})(Dialog);
